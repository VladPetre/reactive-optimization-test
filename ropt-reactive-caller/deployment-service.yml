apiVersion: apps/v1
kind: Deployment
metadata:
  name: ropt-r-caller
  namespace: ropt
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ropt-r-caller
  template:
    metadata:
      name: ropt-r-caller-pod
      labels:
        app: ropt-r-caller
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: ropt-r-caller-container
          image: vladstep/ropt-r-caller:latest
          imagePullPolicy: Always
          ports:
            - name: caller-r-port
              containerPort: 8080
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "1000m"