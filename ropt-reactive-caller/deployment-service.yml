apiVersion: apps/v1
kind: Deployment
metadata:
  name: ropt-react-caller
  namespace: ropt
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ropt-react-caller
  template:
    metadata:
      name: ropt-react-caller-pod
      labels:
        app: ropt-react-caller
        team: vsp
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: ropt-react-caller-container
          image: vladstep/ropt-react-caller:{{ropt-image-tag}}
          imagePullPolicy: Always
          ports:
            - name: caller-port
              containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: ropt-react-caller-cip-svc
  namespace: ropt
  labels:
    app: ropt-react-caller-cip
    vsp.monitor: caller-react-service
spec:
  selector:
    app: ropt-react-caller
  type: ClusterIP
  ports:
    - name: caller-r-port
      port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: ropt-react-caller-svc
  namespace: ropt
  labels:
    app: ropt-react-caller
spec:
  selector:
    app: ropt-react-caller
  type: LoadBalancer
  ports:
    - port: 8081
      targetPort: 8080