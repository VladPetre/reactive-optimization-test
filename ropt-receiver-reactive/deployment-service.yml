apiVersion: apps/v1
kind: Deployment
metadata:
  name: ropt-r-receiver
  namespace: ropt
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ropt-r-receiver
  template:
    metadata:
      name: ropt-r-receiver-pod
      labels:
        app: ropt-r-receiver
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: ropt-r-receiver-container
          image: vladstep/ropt-receiver-reactive:latest
          imagePullPolicy: Always
          ports:
            - name: receiver-r-port
              containerPort: 8080
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "1000m"