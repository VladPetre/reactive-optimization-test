---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ropt-receiver-reactive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ropt-receiver-reactive
  template:
    metadata:
      name: ropt-receiver-reactive-pod
      labels:
        app: ropt-receiver-reactive
    spec:
      containers:
        - name: ropt-receiver-reactive-container
          image: vladstep/ropt-receiver-reactive:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: ropt-receiver-reactive-cip-svc
  labels:
    app: ropt-receiver-reactive
spec:
  selector:
    app: ropt-receiver-reactive
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: ropt-receiver-reactive-svc
  labels:
    app: ropt-receiver-reactive
spec:
  selector:
    app: ropt-receiver-reactive
  type: LoadBalancer
  ports:
    - port: 9090
      targetPort: 8080