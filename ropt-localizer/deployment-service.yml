apiVersion: apps/v1
kind: Deployment
metadata:
  name: ropt-localizer
  namespace: ropt
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ropt-localizer
  template:
    metadata:
      name: ropt-localizer-pod
      labels:
        app: ropt-localizer
        team: vsp
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: ropt-localizer-container
          image: vladstep/ropt-localizer:latest
          imagePullPolicy: Always
          ports:
            - name: localizer-port
              containerPort: 8080
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "1000m"
